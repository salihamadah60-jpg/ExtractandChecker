- [Keyword filter integration](keyword-filter-integration.md) — keywordFilter.isAdOnlySync/isBannedSync must be called with (workspaceId, combinedText) inside _runPipeline; init() must be called in server/index.ts startup.
- [Reader cumulative counters](reader-cumulative-counters.md) — totalMessages/totalLinksFound/totalLinksNew/totalPipelineRuns are persisted in MongoDB via systemState; reset via POST /api/reader/reset-counters which also updates in-memory stats immediately.
- [User activity tracking — per-workspace](user-activity-tracking.md) — _lastUserActivity was global causing cross-workspace interference; fixed with _lastUserActivityByWorkspace Map + isUserActiveForWorkspace(wid).
- [Workspace isolation + admin aggregation](workspace-isolation-admin.md) — NEVER auto-populate new workspaces from CentralLinks. Admin workspace = "main" only. Regular workspaces: TTL 30 days, isolated. Admin gets all filtered groups pushed automatically.
- [Threat monitor system](threat-monitor.md) — server/modules/threat-monitor.ts tracks per-workspace rateLimitCount/stopJoinCount/kickCount; levels LOW/MEDIUM/HIGH/CRITICAL; daily limit 55/day hard stop; join-manager integrates via recordRateLimit/recordStopJoin/recordKick/recordSuccessfulJoin; routes GET /api/join/threat-level + POST /api/join/safety-mode; stopType:"rate_limit" distinguishes 429 from 421 in joinOne return.
